diff --git a/node_modules/@sudowealth/schwab-api/dist/src/trader/accounts/schema.d.ts b/node_modules/@sudowealth/schwab-api/dist/src/trader/accounts/schema.d.ts
index 44082e8..eb2ed08 100644
--- a/node_modules/@sudowealth/schwab-api/dist/src/trader/accounts/schema.d.ts
+++ b/node_modules/@sudowealth/schwab-api/dist/src/trader/accounts/schema.d.ts
@@ -2,20 +2,20 @@ import { z } from 'zod';
 export declare const AccountsBaseInstrument: z.ZodObject<{
     assetType: z.ZodEnum<["EQUITY", "MUTUAL_FUND", "OPTION", "FUTURE", "FOREX", "INDEX", "CASH_EQUIVALENT", "FIXED_INCOME", "PRODUCT", "CURRENCY", "COLLECTIVE_INVESTMENT"]>;
     cusip: z.ZodOptional<z.ZodString>;
-    symbol: z.ZodString;
+    symbol: z.ZodOptional<z.ZodString>;
     description: z.ZodOptional<z.ZodString>;
     instrumentId: z.ZodOptional<z.ZodNumber>;
     netChange: z.ZodOptional<z.ZodNumber>;
 }, "strip", z.ZodTypeAny, {
-    symbol: string;
     assetType: "EQUITY" | "FOREX" | "FUTURE" | "INDEX" | "MUTUAL_FUND" | "OPTION" | "CASH_EQUIVALENT" | "FIXED_INCOME" | "PRODUCT" | "CURRENCY" | "COLLECTIVE_INVESTMENT";
+    symbol?: string | undefined;
     description?: string | undefined;
     cusip?: string | undefined;
     netChange?: number | undefined;
     instrumentId?: number | undefined;
 }, {
-    symbol: string;
     assetType: "EQUITY" | "FOREX" | "FUTURE" | "INDEX" | "MUTUAL_FUND" | "OPTION" | "CASH_EQUIVALENT" | "FIXED_INCOME" | "PRODUCT" | "CURRENCY" | "COLLECTIVE_INVESTMENT";
+    symbol?: string | undefined;
     description?: string | undefined;
     cusip?: string | undefined;
     netChange?: number | undefined;
diff --git a/node_modules/@sudowealth/schwab-api/dist/src/trader/accounts/schema.js b/node_modules/@sudowealth/schwab-api/dist/src/trader/accounts/schema.js
index 4c1ca3f..37958b3 100644
--- a/node_modules/@sudowealth/schwab-api/dist/src/trader/accounts/schema.js
+++ b/node_modules/@sudowealth/schwab-api/dist/src/trader/accounts/schema.js
@@ -4,7 +4,7 @@ import { assetType, AccountAPIOptionDeliverable } from '../shared';
 export const AccountsBaseInstrument = z.object({
     assetType: assetType,
     cusip: z.string().optional(), // Made optional based on some TransactionInstrument variations
-    symbol: z.string(),
+    symbol: z.string().optional(), // Made optional because cash equivalents may not have symbols
     description: z.string().optional(),
     instrumentId: z.number().int().optional(),
     netChange: z.number().optional(),
@@ -21,7 +21,7 @@ const AccountEquity = AccountsBaseInstrument.extend({
 });
 const AccountFixedIncome = AccountsBaseInstrument.extend({
     assetType: z.literal(assetType.Enum.FIXED_INCOME),
-    maturityDate: z.string().datetime().optional(),
+    maturityDate: z.string().optional(), // Accepts both date (YYYY-MM-DD) and datetime formats
     factor: z.number().optional(),
     variableRate: z.number().optional(),
 });
diff --git a/node_modules/@sudowealth/schwab-api/dist/src/trader/orders/schema.js b/node_modules/@sudowealth/schwab-api/dist/src/trader/orders/schema.js
index 99a31a9..4f91b63 100644
--- a/node_modules/@sudowealth/schwab-api/dist/src/trader/orders/schema.js
+++ b/node_modules/@sudowealth/schwab-api/dist/src/trader/orders/schema.js
@@ -279,89 +279,89 @@ const OrderActivity = z.object({
     executionLegs: z.array(ExecutionLeg),
 });
 const OrderLegCollection = z.object({
-    orderLegType: orderLegType,
-    legId: z.number().int(),
+    orderLegType: orderLegType.optional(),
+    legId: z.number().int().optional(),
     instrument: z.lazy(() => AccountsInstrument), // Use lazy for potential circular dependency
     instruction: instruction,
-    positionEffect: positionEffect,
+    positionEffect: positionEffect.optional(),
     quantity: z.number(),
-    quantityType: quantityType,
-    divCapGains: divCapGains,
-    toSymbol: z.string(),
+    quantityType: quantityType.optional(),
+    divCapGains: divCapGains.optional(),
+    toSymbol: z.string().optional(),
 });
 const OrderRequest = z.object({
     session: session,
     duration: duration,
     orderType: orderTypeRequest,
-    cancelTime: z.string().datetime(),
-    complexOrderStrategyType: complexOrderStrategyType,
-    quantity: z.number(),
-    filledQuantity: z.number(), // Typically not part of a request, but included per spec
-    remainingQuantity: z.number(), // Typically not part of a request, but included per spec
-    destinationLinkName: z.string(),
-    releaseTime: z.string().datetime(),
-    stopPrice: z.number(),
-    stopPriceLinkBasis: stopPriceLinkBasis,
-    stopPriceLinkType: stopPriceLinkType,
-    stopPriceOffset: z.number(),
-    stopType: stopType,
-    priceLinkBasis: priceLinkBasis,
-    priceLinkType: priceLinkType,
-    price: z.number(),
-    taxLotMethod: taxLotMethod,
+    cancelTime: z.string().optional(), // Accepts any datetime format
+    complexOrderStrategyType: complexOrderStrategyType.optional(),
+    quantity: z.number().optional(),
+    filledQuantity: z.number().optional(), // Typically not part of a request, but included per spec
+    remainingQuantity: z.number().optional(), // Typically not part of a request, but included per spec
+    destinationLinkName: z.string().optional(),
+    releaseTime: z.string().optional(), // Accepts any datetime format
+    stopPrice: z.number().optional(),
+    stopPriceLinkBasis: stopPriceLinkBasis.optional(),
+    stopPriceLinkType: stopPriceLinkType.optional(),
+    stopPriceOffset: z.number().optional(),
+    stopType: stopType.optional(),
+    priceLinkBasis: priceLinkBasis.optional(),
+    priceLinkType: priceLinkType.optional(),
+    price: z.number().optional(),
+    taxLotMethod: taxLotMethod.optional(),
     orderLegCollection: z.array(OrderLegCollection),
-    activationPrice: z.number(),
-    specialInstruction: specialInstruction,
+    activationPrice: z.number().optional(),
+    specialInstruction: specialInstruction.optional(),
     orderStrategyType: orderStrategyType,
-    orderId: z.number().int(), // Optional for new orders
-    cancelable: z.boolean().default(false), // Typically not part of a request
-    editable: z.boolean().default(false), // Typically not part of a request
-    status: status, // Typically not part of a request
-    enteredTime: z.string().datetime(), // Typically set by server
-    closeTime: z.string().datetime(), // Typically set by server
-    accountNumber: z.string(), // Must match path param, but often optional in body?
-    orderActivityCollection: z.array(OrderActivity), // Typically not part of a request
-    replacingOrderCollection: z.array(z.object({})), // Placeholder
-    childOrderStrategies: z.array(z.object({})), // Placeholder
-    statusDescription: z.string(), // Typically not part of a request
+    orderId: z.number().int().optional(), // Optional for new orders
+    cancelable: z.boolean().default(false).optional(), // Typically not part of a request
+    editable: z.boolean().default(false).optional(), // Typically not part of a request
+    status: status.optional(), // Typically not part of a request
+    enteredTime: z.string().optional(), // Typically set by server, accepts any datetime format
+    closeTime: z.string().optional(), // Typically set by server, accepts any datetime format
+    accountNumber: z.string().optional(), // Must match path param, but often optional in body?
+    orderActivityCollection: z.array(OrderActivity).optional(), // Typically not part of a request
+    replacingOrderCollection: z.array(z.object({})).optional(), // Placeholder
+    childOrderStrategies: z.array(z.object({})).optional(), // Placeholder
+    statusDescription: z.string().optional(), // Typically not part of a request
 });
 const Order = z.object({
     session: session,
     duration: duration,
     orderType: orderType,
-    cancelTime: z.string().datetime(),
-    complexOrderStrategyType: complexOrderStrategyType,
-    quantity: z.number(),
-    filledQuantity: z.number(),
-    remainingQuantity: z.number(),
-    requestedDestination: requestedDestination,
-    destinationLinkName: z.string(),
-    releaseTime: z.string().datetime(),
-    stopPrice: z.number(),
-    stopPriceLinkBasis: stopPriceLinkBasis,
-    stopPriceLinkType: stopPriceLinkType,
-    stopPriceOffset: z.number(),
-    stopType: stopType,
-    priceLinkBasis: priceLinkBasis,
-    priceLinkType: priceLinkType,
-    price: z.number(),
-    taxLotMethod: taxLotMethod,
+    cancelTime: z.string().optional(), // Accepts any datetime format
+    complexOrderStrategyType: complexOrderStrategyType.optional(),
+    quantity: z.number().optional(),
+    filledQuantity: z.number().optional(),
+    remainingQuantity: z.number().optional(),
+    requestedDestination: requestedDestination.optional(),
+    destinationLinkName: z.string().optional(),
+    releaseTime: z.string().optional(), // Accepts any datetime format
+    stopPrice: z.number().optional(),
+    stopPriceLinkBasis: stopPriceLinkBasis.optional(),
+    stopPriceLinkType: stopPriceLinkType.optional(),
+    stopPriceOffset: z.number().optional(),
+    stopType: stopType.optional(),
+    priceLinkBasis: priceLinkBasis.optional(),
+    priceLinkType: priceLinkType.optional(),
+    price: z.number().optional(),
+    taxLotMethod: taxLotMethod.optional(),
     orderLegCollection: z.array(OrderLegCollection),
-    activationPrice: z.number(),
-    specialInstruction: specialInstruction,
+    activationPrice: z.number().optional(),
+    specialInstruction: specialInstruction.optional(),
     orderStrategyType: orderStrategyType,
-    orderId: z.number().int(),
-    cancelable: z.boolean().default(false),
-    editable: z.boolean().default(false),
-    status: status,
-    enteredTime: z.string().datetime(),
-    closeTime: z.string().datetime(),
-    tag: z.string(),
-    accountNumber: z.number().int(),
-    orderActivityCollection: z.array(OrderActivity),
-    replacingOrderCollection: z.array(z.object({})), // Placeholder for unknown structure
-    childOrderStrategies: z.array(z.object({})), // Placeholder for unknown structure
-    statusDescription: z.string(),
+    orderId: z.number().int().optional(),
+    cancelable: z.boolean().default(false).optional(),
+    editable: z.boolean().default(false).optional(),
+    status: status.optional(),
+    enteredTime: z.string().optional(), // Accepts any datetime format
+    closeTime: z.string().optional(), // Accepts any datetime format
+    tag: z.string().optional(),
+    accountNumber: z.number().int().optional(),
+    orderActivityCollection: z.array(OrderActivity).optional(),
+    replacingOrderCollection: z.array(z.object({})).optional(), // Placeholder for unknown structure
+    childOrderStrategies: z.array(z.object({})).optional(), // Placeholder for unknown structure
+    statusDescription: z.string().optional(),
 });
 export const GetOrdersResponseBody = z.array(Order);
 // --- GET /orders endpoint schemas ---
